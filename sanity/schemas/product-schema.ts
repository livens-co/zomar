const product = {
  name: "products",
  title: "Proizvodi",
  type: "document",
  fields: [
    {
      name: "title",
      title: "Naziv proizvoda",
      type: "string",
      required: true,
    },
  ],
};

export default product;